import { LoaderContainer } from "./styledComponents";

const Loader = () => {
    return (
        <LoaderContainer>
            Loading...
        </LoaderContainer>

    );
};
export default Loader;